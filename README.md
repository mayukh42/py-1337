py-1337
==============

Yet another py code repo
--------------


Run tests
--------------

Create a virtualenv, add current project root to PYTHONPATH, then run the tests
```
    $ python -m venv .venv
    $ export PYTHONPATH="$(pwd):$PYTHONPATH"
    $ python tests/item.py
```

TBA
