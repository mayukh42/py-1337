py-1337
==============

Yet another py code repo
--------------


Run tests
--------------

Clone repo, Create a virtualenv, add current project root to PYTHONPATH, then run the tests
``` 
    $ git clone https://github.com/mayukh42/py-1337.git
    $ python -m venv .venv
    $ export PYTHONPATH="$(pwd):$PYTHONPATH"
    $ python tests/item.py
```

TBA
